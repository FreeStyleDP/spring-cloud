server:
  port: 8001

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/clouddb01?useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: root

# mybatis-plus
mybatis-plus:
  global-config:
    db-config:
      id-type: UUID
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
